# OpenAI API Configuration
# Required: Your OpenAI API key with access to Chat Completions and TTS endpoints
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
# Optional: Port number for the server to listen on
PORT=8080

# Model Configuration
# Optional: OpenAI model to use for chat completions (default: gpt-4o-mini)
LLM_MODEL=gpt-4o-mini

# Optional: OpenAI model to use for text-to-speech (default: tts-1)
TTS_MODEL=tts-1
